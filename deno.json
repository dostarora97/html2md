{
  "name": "@placeholder/html2md",
  "version": "0.1.0",
  "exports": "./mod.ts",
  "tasks": {
    "install": "deno install --global --allow-read --allow-env --force --config deno.json --name html2md ./cli.ts",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "lint": "deno lint",
    "test": "deno test --allow-read --allow-env",
    "check": "deno check mod.ts cli.ts",
    "ci": "deno task fmt:check && deno task lint && deno task check && deno task test",
    "publish-dry": "deno publish --dry-run",
    "publish": "deno publish"
  },
  "compilerOptions": {
    "lib": ["deno.window", "dom"]
  },
  "imports": {
    "turndown": "npm:turndown@7.2.2",
    "@mozilla/readability": "npm:@mozilla/readability@0.6.0",
    "linkedom": "npm:linkedom@0.18.10",
    "@std/assert": "jsr:@std/assert@1.0.14"
  },
  "exclude": ["node_modules"]
}
