let's go with housekeeping for this particular fix and publish a new semver marked (if needed).
and keep others in the todo and then we will address them later.

---

don't use sap-github-mcp, because that is particularly only for projects under $TPP and nothing else. for all the other project we gotta use the `gh`. you should not even have the reference for `sap-github-mcp` and you should just use `gh` directly.

---

you can continue with any of your open 4 tasks that you were doin and mark them complete once done or obselete